import styled from 'styled-components';

export const Container = styled.div`
  overflow-x: scroll;
  white-space: nowrap;

  margin-bottom: 10px;
  ::-webkit-scrollbar {
    display: none;
  }
  padding-bottom: 10px;
`;
