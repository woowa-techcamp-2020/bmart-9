import styled from 'styled-components';

export const Container = styled.div`
  overflow-x: scroll;
  white-space: nowrap;

  ::-webkit-scrollbar {
    display: none;
  }
  margin: 0 0 10px 2vw;
  padding-bottom: 3px;
`;
